import hashlib
import os

# Generate a random string

random_string = os.urandom(32)

# Hash the random string using SHA-256
hashed_string = hashlib.sha256(random_string).hexdigest()

print(hashed_string)