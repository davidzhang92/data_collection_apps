/*!
 * Built with Duet Design System
 */
'use strict';

const index = require('./index-d380d72b.js');

const focusElement = (element) => {
  if ("setFocus" in element) {
    element.setFocus();
  }
  else {
    element.focus();
  }
};
const FocusGuard = ({ moveFocusTo }) => {
  return index.h("div", { class: "duet-focus-guard", "aria-hidden": "true", tabIndex: 0, onFocus: () => focusElement(moveFocusTo) });
};

exports.FocusGuard = FocusGuard;
exports.focusElement = focusElement;
