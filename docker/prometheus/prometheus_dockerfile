# docker buildx build -t prom/prometheus -f prometheus_dockerfile .
# docker run -d --name=prometheus -p 9091:9090 prom/prometheus

FROM prom/prometheus:main AS base
EXPOSE 9091
COPY ./docker/prometheus/prometheus.yml /etc/prometheus/prometheus.yml